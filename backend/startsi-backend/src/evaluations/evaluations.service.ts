import { Injectable } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service';
@Injectable() export class EvaluationsService { constructor(private prisma:PrismaService){} list(){return this.prisma.evaluation.findMany({include:{startup:true,challenge:true,evaluator:true}});} async submit(data:any){const total=Object.values(data.scores||{}).reduce<number>((n,x:any)=>n+(Number(x.score)*Number(x.weight||1)),0);return this.prisma.evaluation.upsert({where:{challengeId_startupId:{challengeId:data.challengeId,startupId:data.startupId}},create:{id:`eval-${Date.now()}`,challengeId:data.challengeId,startupId:data.startupId,evaluatorId:data.evaluatorId,scores:data.scores,totalScore:total,comment:data.comment,status:data.status||'SUBMITTED'},update:{scores:data.scores,totalScore:total,comment:data.comment,status:data.status||'SUBMITTED'}});} }
